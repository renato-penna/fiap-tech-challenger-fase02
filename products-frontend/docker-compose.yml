version: '3'

services:

  app:
    container_name: products-frontend
    build: .
    command: runserver
    volumes:
      - .:/app
    env_file:
      - .env
    ports:
      - "8501:8501"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s


